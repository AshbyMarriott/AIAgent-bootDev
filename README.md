# AIAgent-bootDev
AI Agent guided project from the associated Boot.Dev course. Uses Google Gemini.

Gives the LLM tools to get files info, read, write, and run python files. Limits access
to the /calculator directory as the working directory for the agent.